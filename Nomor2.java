import java.util.Scanner;

public class Nomor2 {
    public static void main(String[] args){
        Scanner input = new Scanner(System.in);
        int N = input.nextInt();
        string transaksi = input.nextLine()
        /* Pekerjaan anda mulai dari sini */
        String transaksi = input.nextLine()

       ArrayList<String> daftarBuku = new ArrayList<>();
       Scanner scanner= new Scanner(System.in);
       String Buku;

       System.out.println("Masukkan pilihan buku [1/2/3/4]:");
       String bukuString = input.nextLine();
       input.nextLine();

       String pilihan;

        System.out.print("Input lagi [Y/N]? ");
        pilihan = scanner.nextLine();

       if (total < 200000) {
            return 0;
        } else if (total <= 500000) {
            return 50000;
        } else if (total <= 750000) {
            return 100000;
        } else if (total <= 1000000) {
            return 150000;
        } else {
            return 200000;
        }
    }
}
        

     }
}
        /* Pekerjaan anda berakhir sini */

        System.out.println(Nomor2.class.getDeclaredMethods().length); 
    }
}
